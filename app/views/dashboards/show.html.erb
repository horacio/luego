<%= form_for @dashboard.new_link do |f| %>
  <%= f.text_field :url, id: 'url', placeholder: 'Save this for later...' %>
  <%= f.submit 'Add Link' %>
<% end %>

<ul id="links">
  <%= render @dashboard.links %>
</ul>
